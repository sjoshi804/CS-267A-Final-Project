from setuptools import setup

setup(name='gym_pursuit_evasion',
      version='0.0.1',
      install_requires=['pycolab', 'gym', 'gym-pycolab','numpy', 'scipy', 'matplotlib']
      #Add any other dependencies required
      #Do we need these here? 'torch', 'torchvision', 'pyro-ppl', 
)